{
  "id": "kanban",
  "name": "Kanban",
  "version": "1.0.0",
  "description": "Allows agents to interact with the Kanban board: search tasks, pick up free-pick tasks, update status, and release tasks.",
  "brief": "You can manage your tasks on the Kanban board using the `kanban` skill. Load it with `use_skill('kanban')` to search, create, update, and track tasks. Use this to stay organized and track your work progress.",
  "author": "Evonic",
  "tools_file": "tools.json",
  "lazy_tools": true,
  "variables": [
    {
      "name": "create_task_super_only",
      "label": "Create Task (Super Agent Only)",
      "type": "boolean",
      "default": true,
      "description": "When enabled, only super agents can create new tasks. Regular agents will be denied."
    },
    {
      "name": "edit_task_super_only",
      "label": "Edit Task (Super Agent Only)",
      "type": "boolean",
      "default": true,
      "description": "When enabled, only super agents can edit task title, description, or priority. Status updates remain controlled by the assignee check."
    },
    {
      "name": "delete_task_super_only",
      "label": "Delete Task (Super Agent Only)",
      "type": "boolean",
      "default": true,
      "description": "When enabled, only super agents can delete tasks. Regular agents will be denied even before the human-approval prompt."
    }
  ]
}
